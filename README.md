# AI vs Real Image Detector

A Streamlit web app that detects whether an image is AI-generated or real using a fine-tuned ResNet50 model.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
